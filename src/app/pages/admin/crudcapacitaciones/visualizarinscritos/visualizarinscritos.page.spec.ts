import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VisualizarinscritosPage } from './visualizarinscritos.page';

describe('VisualizarinscritosPage', () => {
  let component: VisualizarinscritosPage;
  let fixture: ComponentFixture<VisualizarinscritosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(VisualizarinscritosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
