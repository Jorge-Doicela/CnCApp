echo "Verificando proyecto antes de commit..."

# 1. Verificación del sistema (No bloqueante)
npm run system:check || echo "Advertencia: System check falló."

# 2. Lint Backend (Permitir continuar si solo hay warnings)
echo "Verificando Backend..."
cd backend && (npm run lint || echo "Advertencia: El Backend tiene avisos de estilo (warnings).")

# 3. Lint Frontend (Comentado porque 'ng lint' no está configurado aún)
echo "Saltando Frontend Lint (no configurado)..."
# cd ../frontend && npm run lint
